#include <iostream>
using namespace std;
#include <vector>
#include "BubbleSort.h"
#include "Sort.h"

 vector<int>BubbleSort::sort(vector<int> list){
    bubbleSort(list);
    return list;

};



void BubbleSort::bubbleSort(vector<int>& array){
    void bubbleSort(vector<int>& array); {
     for (int i = array.size() - 1; i > 0; i--) {
          for(int j = 0; j < i; j++) {
          if(array.at(j) > array.at(j+1)) {
         } 
        }
      }
    }  

}